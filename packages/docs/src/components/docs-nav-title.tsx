"use client";

/*
 * The docs sidebar's title slot, emptied. The wordmark is already on the page
 * in the site navbar one row up; a second one in the sidebar would sit
 * directly beneath it.
 */
export function DocsNavTitle() {
  return null;
}
