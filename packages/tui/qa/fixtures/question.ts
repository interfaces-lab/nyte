// Installed as a normal project plugin. This is Nyte's exported question example,
// not a replacement tool implementation or a hook inside the compiled binary.
import { questionPlugin } from "@nyte-ai/plugin/examples/question";

export default questionPlugin;
