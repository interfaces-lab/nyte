/** Notification lifecycle lives in Sonner; each client supplies its surface tokens. */
export { Toaster, toast, useSonner } from "sonner";

export type { ExternalToast, ToasterProps } from "sonner";
