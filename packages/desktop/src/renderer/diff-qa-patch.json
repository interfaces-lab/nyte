"--- packages/desktop/src/renderer/src/session-actions.test.ts\n+++ packages/desktop/src/renderer/src/session-actions.test.ts\n@@ -60,10 +60,10 @@\n   return { client, saved, writes, failures, actions, directory };\n }\n \n function notification(title: string) {\n-  const notice = toast.getToasts().find((item) => item.title === title);\n-  assert.ok(notice, `Missing notification: ${title}`);\n+  const notice = toast.getToasts().find((item) => \"title\" in item && item.title === title);\n+  assert.ok(notice !== undefined && \"title\" in notice, `Missing notification: ${title}`);\n   return notice;\n }\n \n function undo(title: string): void {\n@@ -80,8 +80,9 @@\n   vi.stubGlobal(\"requestAnimationFrame\", (callback: FrameRequestCallback) => {\n     callback(performance.now());\n     return 0;\n   });\n+  vi.stubGlobal(\"cancelAnimationFrame\", () => {});\n   toast.dismiss();\n });\n afterEach(() => vi.unstubAllGlobals());\n \n"
