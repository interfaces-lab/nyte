1. [tools/ls.ts:107](/Users/workgyver/Developer/nyte/packages/core/src/tools/ls.ts:107) — **Regression.** Slicing before `stat` makes unreadable entries consume the limit. With `limit: 1`, an unreadable first entry hides a readable second entry and reports “empty directory.” Restore the sequential loop or process bounded batches until enough successful entries exist. Also reject negative/fractional limits with `Type.Integer({ minimum: 1 })`; `slice` changed their behavior.

2. [tools/support/arguments.ts:15](/Users/workgyver/Developer/nyte/packages/core/src/tools/support/arguments.ts:15) — **Accept.** `Clean` then `Check` is strict about declared values while intentionally stripping unknown keys. Current schemas reject coercible strings. `{}` is persistence-safe: canonical JSON already drops undefined-valued properties. No presence-sensitive consumer found. The export removals, summary folding, queue tie-breaking, truncation reversal, effect-clear reuse, and media-type extraction show no additional regression in the inspected paths.

3. [views/changes.ts:91](/Users/workgyver/Developer/nyte/packages/core/src/kernel/views/changes.ts:91) — **G is the best small cleanup.** Give the builder a lazily initialized path→index map, preserving array order. In [edit-diff.ts:331](/Users/workgyver/Developer/nyte/packages/core/src/tools/edit-diff.ts:331), reuse normalized content and needles across matching and duplicate counting. Preserve fuzzy duplicate detection even when exact matching succeeds.

4. [graph.ts:18](/Users/workgyver/Developer/nyte/packages/core/src/kernel/graph.ts:18) — **A is the best broad performance change.** Prefer a recursive-CTE chain read, initially using the existing JSON parent. It removes round trips, not O(H) traversal or decoding. Preserve missing-object, wrong-kind, cycle, and hash checks; retain checkpoint stopping. Persistent tip caching is not trivially correct: [the existing test](/Users/workgyver/Developer/nyte/packages/core/test/kernel/sdk-snapshot-reads.test.ts:196) requires detecting deleted ancestry on a subsequent snapshot. Caching every complete tip array also risks quadratic memory.

5. [core/src/client/session-follow.ts:184](/Users/workgyver/Developer/nyte/packages/core/src/client/session-follow.ts:184) — **B is third priority, after G and A.** Every matching event waits for a full snapshot whose transcript is discarded. Coalesce metadata refreshes and separate them from event publication, preserving selection guards and checkpoint updates. That reduces redundant calls; a lightweight projection read is needed to remove transcript construction entirely.

6. [nyte.ts:1511](/Users/workgyver/Developer/nyte/packages/core/src/kernel/sdk/nyte.ts:1511) — **C next; D defer.** Process sessions in bounded batches while preserving pagination/filter order. Keep pending-before-head ordering at `:601`. Seed `pooled.createdAt` from listing metadata to avoid another listing on the next `get`. Removing the child scans at `:2273`/`:2437` needs persistent parent indexing; a pool-only index misses unopened children.

7. [events.ts:97](/Users/workgyver/Developer/nyte/packages/core/src/kernel/sdk/events.ts:97) — **E: reject timestamp pruning.** Commit timestamps have no enforced ancestry monotonicity. A smaller walk bound is valid only when every consumer resnapshots on fallback. For **F**, [sqlite.ts:490](/Users/workgyver/Developer/nyte/packages/core/src/kernel/sqlite.ts:490) can batch deletes later; retain `parseObject` hashing because it checks integrity.

8. [nyte.ts:243](/Users/workgyver/Developer/nyte/packages/core/src/kernel/sdk/nyte.ts:243) — **H after A, incrementally.** Approximate file map: `session-pool.ts` 450–550 lines; `runner.ts` 350–450; `subagent-host.ts` 300–350; `relocate.ts` 140–170; `summaries.ts` 300–350; remaining `nyte.ts` 650–800. Move lifecycle state with its owner. `runnerDone` is disposer-keyed, not SessionId-keyed; preserve tracking of unfinished retired runners. Split compaction’s pure summarization from lease/publication handling separately.

9. [edit-diff.ts:112](/Users/workgyver/Developer/nyte/packages/core/src/tools/edit-diff.ts:112) — **Missed cheap improvement.** Sorted, nonoverlapping replacements permit one forward slice-and-join pass instead of rebuilding the file per edit. Also inspect [nyte.ts:753](/Users/workgyver/Developer/nyte/packages/core/src/kernel/sdk/nyte.ts:753): effect routing sequentially reads run objects. Parallel reads preserve first-match order. Remaining stream `shift()` calls are lower priority because their buffers are bounded.

10. [compaction.ts:1184](/Users/workgyver/Developer/nyte/packages/core/src/kernel/compaction.ts:1184) — **I confirmed in source.** Move outcome handling outside `finally`, then aggregate cleanup failures after cleanup completes. Coordinate with its owner.

Read-only inspection plus focused runtime reproduction; full tests, lint, and knip were not rerun.