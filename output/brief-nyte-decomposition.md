Repository: /Users/workgyver/Developer/nyte (pnpm monorepo, TypeScript 7, Node 26). Read /Users/workgyver/Developer/nyte/AGENTS.md and /Users/workgyver/Developer/nyte/packages/core/AGENTS.md first and follow them strictly: no `any`, no `as` casts (only `as const`), no enums/parameter properties/namespaces, top-level imports only with `.ts` extensions, no star or renamed imports, `const _exhaustive: never` in default arms, one options object for 3+ inputs, never run `git stash`. Other agents edit this tree concurrently: touch only packages/core/src/kernel/sdk/** (nyte.ts and the new files you create beside it), packages/core/src/index.ts if an export path must move, packages/core/src/kernel/README.md (the `sdk/` row), and tests under packages/core/test that reference what you move.

Read first: packages/core/src/kernel/README.md, packages/core/src/kernel/sdk/types.ts (the public `Nyte` contract; do not change it), packages/core/src/kernel/sdk/nyte.ts (2 500 lines, one `createNyte` closure), packages/core/src/kernel/sdk/jobs.ts and activation.ts (siblings that already follow the "closure returning an object of methods" style you should match).

Goal: decompose `createNyte` into cohesive modules without changing observable behavior, and fix `sessions.list` cost. This is a pi-style readability rework (github.com/earendil-works/pi): every file should read top-to-bottom as one concept.

Target file map (line counts are estimates; adjust, but keep every file under ~800 lines and nyte.ts under ~700):
- `sdk/session-pool.ts` (~450–550): the `Pooled` record and everything that owns a pooled session: `adopt`, `openPooled`, `retire` (the body of `sessions.delete` cleanup), facts (`readFact`, `writeFact`, `writeBlobRef`, `readFacts`, `storedCwd`), `createdAtFor`, `readRun`, `currentRun`, `parkedCalls`, `listSessionHeads`, `projectHeads`, `readSession`, activation resolution (`resolveHostActivation`, `resolveSessionActivation`, `activationFor`, `activeFor`, `clientActivation`, `catalogForNewSession`), notice fan-out (`dispatchNotice`, `subscribeNotices`). Fold the SessionId-keyed side maps that live in `createNyte` today (`reconciliations`, `runnerTasks`, `driveStates`, `noticeListeners`) into fields on `Pooled` where the value is per-session; `runnerDone` is keyed by disposer, not session, so keep it with the runner and make sure a retired session's unfinished runner is still awaited on close (read `sessions.delete` and `close()` carefully before moving them).
- `sdk/runner.ts` (~350–450): `createRunner` (drive loop, `wake`, `inspectRunEvent`, `handleRef`, `headForRun`, the question-tool guard), `stopRunner`, `reconcileRunner`, `requestAbortAtRef`, `abortLocalDrive`, `emitRunnerDiagnostic`, `runAtRef`, plus `selectedAttachment` and the `Attachment` set, or pass a `covers(id, pooled)` callback in if that reads cleaner.
- `sdk/subagent-host.ts` (~300–350): `childSessionId`, `openChild`, `childResultText`, `childState`, `interruptChild`, `interruptPooledChild`, `backgroundChild`, `jobsFor`, and the `subagentHost` object (spawn/wait).
- `sdk/relocate.ts` (~150): the `relocate` operation and `sessionCwd`.
- `sdk/summaries.ts` (~300–350): `runs.compact` and the summary half of `heads.move` (`summaryFailure`, `resolveBranchTurn`, the causes/AggregateError handling), exposed as two functions the namespaces call.
- `sdk/nyte.ts` (remaining, ~650): `createNyte` wiring plus the thin namespace methods (`sessions`, `messages`, `jobs`, `runs`, `heads`, `workspace`, `provider`, `plugins`, `watch`, `setPlugins`, `reactivate`, `attach`, `close`) delegating to the modules above.

Method: each module is a factory taking one typed options object (the `NyteOptions` slice it needs plus the shared services it calls) and returning an object of functions, like `createJobs`. Prefer passing a small `SessionPool` interface into runner/subagent/relocate rather than threading many callbacks. Preserve every comment that explains a constraint (there are many: races on adopt, restart delay, "read the tip before the run", etc.); move them with the code. Delete nothing that is behavior. Do not introduce classes with parameter properties.

Also fix C: `sessions.list` (currently a sequential loop that, per session, does openPooled + readFacts + pending + listHeads + a full branch read). Process the page's candidates in bounded parallel batches (e.g. 8 at a time, a tiny helper, no dependency) while preserving: the store's listing order, cursor/`next` semantics (`next` is the index after the last examined session), the `includeArchived`/`parent`/`search` filters, `UnknownSession` skipping, and the comment that pending must be read before the branch ("Read queued choices first so a concurrent landing cannot make one disappear") inside `readSession`. Seed `pooled.createdAt` from the listing row so a later `sessions.get` does not list the store again (that field was added recently; keep it).

Do not change the `Nyte` public types, event projection (`events.ts`, `watch.ts`), `jobs.ts`, `activation.ts`, `snapshot.ts`, or any kernel file outside `sdk/`. Another agent may have just changed `sessions.snapshot` to pass `session.objects` straight into `branch(...)` (a store `objects.chain` query now exists); keep whatever that code looks like when you start.

Verify after each module extraction, not only at the end: `pnpm --dir packages/core typecheck`, then `pnpm --dir packages/core test` (runs both SQLite and worker store backends; ~1 min), `pnpm oxlint packages/core`, `pnpm oxfmt packages/core`. Also run `pnpm --dir packages/tui typecheck` and `pnpm --dir packages/server typecheck` at the end. A pre-existing oxlint `no-unsafe-finally` error in packages/core/src/kernel/compaction.ts and a desktop typecheck error in tool-detail.ts are other agents' work; report, do not fix.

Report: the final file map with real line counts, the interfaces between modules (signatures), what state moved into `Pooled`, how `sessions.list` batches, test results for both backends, and anything you deliberately left in nyte.ts and why.
