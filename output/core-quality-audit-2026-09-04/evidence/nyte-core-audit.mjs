import { prepareCheckpoint, summarizeCheckpoint } from '/Users/workgyver/Developer/nyte/packages/core/src/kernel/compaction.ts';
import { bindTurn } from '/Users/workgyver/Developer/nyte/packages/core/src/kernel/turn.ts';
import { SqliteStore } from '/Users/workgyver/Developer/nyte/packages/core/src/kernel/sqlite.ts';
import { hashObject } from '/Users/workgyver/Developer/nyte/packages/core/src/kernel/hash.ts';
import { createAssistantMessageEventStream } from '/Users/workgyver/Developer/nyte/packages/ai/src/index.ts';
const usage = { input: 1, output: 1, cacheRead: 0, cacheWrite: 0, totalTokens: 2, cost: {input:0,output:0,cacheRead:0,cacheWrite:0,total:0} };
const assistant = (text, stopReason='stop') => ({role:'assistant',content:[{type:'text',text}],api:'openai-responses',provider:'openai',model:'test-model',usage,stopReason,timestamp:1});
const user = text => ({role:'user',content:text,timestamp:1});
const model = {id:'test-model',name:'Test',api:'openai-responses',provider:'openai',baseUrl:'https://example.invalid',reasoning:false,input:['text'],cost:usage.cost,contextWindow:10000,maxTokens:100};
const items = bodies => { let parent = null; return bodies.map(body => { const commit = {kind:'commit',parent,body,at:1}; const oid = hashObject(commit); parent = oid; return {oid,commit}; }); };
const commits = items([{kind:'checkpoint',summary:'CRITICAL_PRIOR_SUMMARY',retainedTail:[user('start of retained turn '.repeat(30)),assistant('early answer '.repeat(30))],tokensBefore:200},{kind:'message',message:assistant('last answer')}]);
const settings = {enabled:true,reserveTokens:100,keepRecentTokens:1};
const prompts = [];
const streamFn = (_model, context) => { prompts.push(JSON.stringify(context)); const stream=createAssistantMessageEventStream(); queueMicrotask(()=>stream.push({type:'done',reason:'stop',message:assistant('new summary')})); return stream; };
const prepared=prepareCheckpoint(commits,settings);
const result=await summarizeCheckpoint({commits,settings,streamFn,model,reason:'manual'});
console.log('compaction',JSON.stringify({split:prepared.value.isSplitTurn,historyMessages:prepared.value.messagesToSummarize.length,previousSummary:prepared.value.previousSummary,summary:result.value?.summary,promptsContainPrevious:prompts.some(p=>p.includes('CRITICAL_PRIOR_SUMMARY'))}));
const store=new SqliteStore(':memory:'); const session=await store.create({id:'audit'}); const lease=(await session.leases.acquire('refs/heads/main',30000)).lease;
const retryTurn=bindTurn({model,systemPrompt:'',tools:[],compaction:{...settings,enabled:false},retry:{enabled:true,maxRetries:3,baseDelayMs:1},streamFn:()=>{const stream=createAssistantMessageEventStream(); const error={...assistant('','error'),errorMessage:'rate limited'}; queueMicrotask(()=>stream.push({type:'error',reason:'error',error})); return stream;}});
const input={session,lease,run:{kind:'run',id:'run',head:'main',phase:{kind:'respond'},startedAt:1,attempts:3,config:{}},attempt:4,commits:items([{kind:'message',message:user('question')}]),signal:new AbortController().signal,emit:()=>{}};
console.log('first failure after three successful rounds',(await retryTurn.respond(input)).kind);
await store.close();
