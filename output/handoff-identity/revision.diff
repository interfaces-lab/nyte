--- pre/packages/protocol/src/kernel.ts
+++ now/packages/protocol/src/kernel.ts
@@ -33,6 +33,11 @@
   readonly imports?: readonly Oid[];
   /** The change this commit landed, when a submission produced it. */
   readonly change?: Oid;
+  /**
+   * The submission key the landed change carried. Correlation for the client
+   * that sent it, never authorization; it stays out of the message content.
+   */
+  readonly key?: string;
   /** The run that wrote this commit, when a runner did. Provenance for per-run views. */
   readonly run?: string;
   readonly body: CommitBody;
--- pre/packages/protocol/src/sdk.ts
+++ now/packages/protocol/src/sdk.ts
@@ -227,6 +227,8 @@
   readonly at: number;
   readonly content: UserMessage["content"];
   readonly author?: Actor;
+  /** The submission key `send` carried, so the sender can match the item to its own outbox by identity. */
+  readonly key?: string;
 }
 
 // ---------------------------------------------------------------------------
--- pre/packages/protocol/src/views.ts
+++ now/packages/protocol/src/views.ts
@@ -13,6 +13,8 @@
   commit: Oid;
   parent: Oid | null;
   content: UserMessage["content"];
+  /** The submission key the landed change carried, when the sender gave one. */
+  key?: string;
 }
 
 export type ToolTurnPart = {
--- pre/packages/protocol/src/schemas.ts
+++ now/packages/protocol/src/schemas.ts
@@ -444,6 +444,7 @@
     parent: nullable(Oid),
     imports: Type.Optional(Type.Array(Oid)),
     change: Type.Optional(Oid),
+    key: Type.Optional(Type.String()),
     run: Type.Optional(Type.String()),
     body: CommitBody,
     at: Type.Number(),
@@ -574,6 +575,7 @@
     at: Type.Number(),
     content: UserContent,
     author: Type.Optional(Actor),
+    key: Type.Optional(Type.String()),
   }),
 );
 
@@ -595,6 +597,7 @@
     commit: Oid,
     parent: nullable(Oid),
     content: UserContent,
+    key: Type.Optional(Type.String()),
   }),
 );
 
--- pre/packages/core/src/kernel/model.ts
+++ now/packages/core/src/kernel/model.ts
@@ -58,6 +58,12 @@
   readonly body: CommitBody;
   readonly at: number;
   readonly author?: Actor;
+  /**
+   * The idempotency key the submission carried. `refs/keys/<key>` answers a
+   * retry; this copy follows the change into its commit so the sender can
+   * recognize its own message by identity after the fact. Correlation only.
+   */
+  readonly key?: string;
 }
 
 export interface RunConfig {
--- pre/packages/core/src/kernel/queue.ts
+++ now/packages/core/src/kernel/queue.ts
@@ -199,8 +199,10 @@
       body: options.body,
       at: Date.now(),
     };
+    const keyed: Change =
+      options.key === undefined ? baseChange : { ...baseChange, key: options.key };
     const change: Change =
-      options.actor === undefined ? baseChange : { ...baseChange, author: options.actor };
+      options.actor === undefined ? keyed : { ...keyed, author: options.actor };
     const oid = onlyOid(await session.objects.put([change]));
     const updates: RefUpdate[] = [{ name: tipName, from: tip, to: oid }];
     if (receiptName !== undefined) {
--- pre/packages/core/src/kernel/step.ts
+++ now/packages/core/src/kernel/step.ts
@@ -166,8 +166,10 @@
       run: runId,
       at: now(),
     };
+    const keyed: Commit =
+      item.change.key === undefined ? baseCommit : { ...baseCommit, key: item.change.key };
     const commit: Commit =
-      item.change.author === undefined ? baseCommit : { ...baseCommit, author: item.change.author };
+      item.change.author === undefined ? keyed : { ...keyed, author: item.change.author };
     previous = hashObject(commit);
     commits.push(commit);
   }
--- pre/packages/core/src/kernel/sdk/snapshot.ts
+++ now/packages/core/src/kernel/sdk/snapshot.ts
@@ -93,9 +93,11 @@
             at: item.change.at,
             content: body.message.content,
           };
+          const keyed =
+            item.change.key === undefined ? pending : { ...pending, key: item.change.key };
           return item.change.author === undefined
-            ? pending
-            : { ...pending, author: item.change.author };
+            ? keyed
+            : { ...keyed, author: item.change.author };
         case "assistant":
         case "toolResult":
           return undefined;
--- pre/packages/core/src/kernel/views/transcript.ts
+++ now/packages/core/src/kernel/views/transcript.ts
@@ -1,5 +1,5 @@
 import { toJsonValue } from "../json.ts";
-import type { Turn, TurnOutcome, TurnPart } from "@nyte-ai/protocol";
+import type { Turn, TurnOutcome, TurnPart, UserTurnPart } from "@nyte-ai/protocol";
 import type { Commit, CommitBody, Oid } from "../model.ts";
 
 type MessageBody = Extract<CommitBody, { kind: "message" }>;
@@ -116,15 +116,19 @@
 
 /** A request opens a turn whatever the commits before it were doing. */
 function appendUser(items: Turn[], item: CommitItem, message: UserMessage): void {
+  const part: UserTurnPart = {
+    kind: "user",
+    commit: item.oid,
+    parent: item.commit.parent,
+    content: message.content,
+  };
   items.push({
     kind: "turn",
     id: item.oid,
     outcome: "completed",
     startedAt: item.commit.at,
     durationMs: 0,
-    parts: [
-      { kind: "user", commit: item.oid, parent: item.commit.parent, content: message.content },
-    ],
+    parts: [item.commit.key === undefined ? part : { ...part, key: item.commit.key }],
   });
 }
 
--- pre/packages/core/src/kernel/README.md
+++ now/packages/core/src/kernel/README.md
@@ -43,7 +43,9 @@
 refs/compactions/<head>        Blob: active checkpoint work fenced by the head lease
 refs/effects/<run>/<call>      Effect: intent -> waiting -> signal/expired -> result
 refs/jobs/<job>                Blob: command or subagent job, output, result, delivery receipt
-refs/keys/<key>                idempotency receipt: the Change a key produced
+refs/keys/<key>                idempotency receipt: the Change a key produced; the change
+                               and the commit that lands it also carry the key, so the
+                               sender can recognize its message by identity
 refs/cancelled/<change>        Blob: a submitted change withdrawn before it landed
 refs/facts/<key>               Blob: a small session value
 refs/deleted                   Blob: the session is being deleted
--- pre/packages/core/test/kernel/queue.test.ts
+++ now/packages/core/test/kernel/queue.test.ts
@@ -110,6 +110,32 @@
   assert.deepEqual(retry, { kind: "duplicate", change: first.change });
   assert.equal((await pending(session, "main")).length, 1);
   assert.equal(await session.events.last(), before);
+});
+
+test("a change carries its submission key, and a redelivered copy keeps it under a new id", async () => {
+  const session = await openSession();
+  const keyed = await submit(session, { head: "main", lane: "now", body: say("hi"), key: "k1" });
+  const bare = await submit(session, { head: "main", lane: "now", body: say("hi") });
+  const keyOf = async (change: string) =>
+    (await pending(session, "main")).find((item) => item.oid === change)?.change.key;
+  assert.equal(await keyOf(keyed.change), "k1");
+  assert.equal(await keyOf(bare.change), undefined);
+
+  const moved = await redeliver(session, { head: "main", change: keyed.change, lane: "later" });
+  assert.equal(moved.kind, "redelivered");
+  if (moved.kind !== "redelivered") return;
+  assert.notEqual(moved.change, keyed.change);
+  const items = await pending(session, "main");
+  const byLane = (lane: string) =>
+    items.filter((item) => item.lane === lane).map((item) => [item.oid, item.change.key]);
+  assert.deepEqual(byLane("later"), [[moved.change, "k1"]]);
+  assert.deepEqual(byLane("now"), [[bare.change, undefined]]);
+  assert.equal(items.length, 2);
+  // The receipt ref still answers the key with the original submission.
+  assert.deepEqual(
+    await submit(session, { head: "main", lane: "now", body: say("hi"), key: "k1" }),
+    { kind: "duplicate", change: keyed.change },
+  );
 });
 
 test("lanes are independent chains, consulted in whatever order the caller names them", async () => {
--- /dev/null
+++ now/packages/core/test/client-session-queue.test.ts
@@ -0,0 +1,116 @@
+/**
+ * The client fold's queue: a landing publishes the head and the queue base in
+ * one CAS, but the watch delivers them as separate frames. The commit that
+ * landed a change must take it out of `pending` itself, so no frame shows a
+ * message both queued and in the transcript.
+ */
+import assert from "node:assert/strict";
+import { test } from "vitest";
+import { foldEvent, stateFromSnapshot } from "../src/client/session-state.ts";
+import type { SessionState } from "../src/client/session-state.ts";
+import {
+  MAIN,
+  sessionId,
+  type PendingItem,
+  type SessionEvent,
+  type SessionSnapshot,
+} from "../src/kernel/sdk/types.ts";
+
+const SESSION = sessionId("queue-fold-test");
+
+function snapshot(pending: readonly PendingItem[]): SessionSnapshot {
+  return {
+    seq: 1,
+    head: MAIN,
+    tip: null,
+    session: {
+      sessionId: SESSION,
+      activation: { kind: "active" },
+      createdAt: 0,
+      lastActivityAt: 0,
+      pinned: false,
+      archived: false,
+      heads: [{ head: MAIN, tip: null }],
+      config: {},
+    },
+    config: {},
+    transcript: [],
+    pending,
+    context: { estimatedTokens: 0, usageTokens: 0, trailingTokens: 0, contextWindow: 1000 },
+  };
+}
+
+const item = (change: string, at: number): PendingItem => ({
+  change,
+  lane: "steer",
+  at,
+  content: `message ${change}`,
+  key: `key-${change}`,
+});
+
+function landing(oid: string, parent: string | null, change: string): SessionEvent {
+  return {
+    seq: 2,
+    kind: "commit",
+    head: MAIN,
+    item: {
+      oid,
+      commit: {
+        kind: "commit",
+        parent,
+        change,
+        key: `key-${change}`,
+        body: {
+          kind: "message",
+          message: { role: "user", content: `message ${change}`, timestamp: 1 },
+        },
+        at: 2,
+      },
+    },
+  };
+}
+
+function applied(state: SessionState, event: SessionEvent): SessionState {
+  const outcome = foldEvent(state, event);
+  assert.equal(outcome.kind, "state");
+  if (outcome.kind !== "state") throw new Error("unreachable");
+  return outcome.state;
+}
+
+function userParts(state: SessionState): readonly (string | undefined)[][] {
+  return state.transcript.items.flatMap((turn) =>
+    turn.kind === "turn"
+      ? turn.parts.flatMap((part) => (part.kind === "user" ? [[part.commit, part.key]] : []))
+      : [],
+  );
+}
+
+test("the commit that lands a change removes it from pending; its landed frame then changes nothing", () => {
+  const start = stateFromSnapshot(snapshot([item("a", 1), item("b", 2)]));
+  const committed = applied(start, landing("c1", null, "a"));
+  assert.deepEqual(userParts(committed), [["c1", "key-a"]]);
+  assert.deepEqual(
+    committed.pending.map((entry) => entry.change),
+    ["b"],
+  );
+  const landed = applied(committed, { seq: 2, kind: "landed", head: MAIN, change: "a" });
+  assert.deepEqual(landed.pending, committed.pending);
+  assert.deepEqual(userParts(landed), [["c1", "key-a"]]);
+});
+
+test("a commit that landed no change, or one this fold never queued, leaves pending alone", () => {
+  const start = stateFromSnapshot(snapshot([item("a", 1)]));
+  const note = applied(start, {
+    seq: 2,
+    kind: "commit",
+    head: MAIN,
+    item: {
+      oid: "n1",
+      commit: { kind: "commit", parent: null, body: { kind: "note", type: "hello" }, at: 2 },
+    },
+  });
+  assert.deepEqual(note.pending, start.pending);
+  const elsewhere = applied(note, landing("c2", "n1", "z"));
+  assert.deepEqual(elsewhere.pending, start.pending);
+  assert.deepEqual(userParts(elsewhere), [["c2", "key-z"]]);
+});
--- pre/packages/tui/src/sent-messages.ts
+++ now/packages/tui/src/sent-messages.ts
@@ -3,82 +3,194 @@
  *
  * The outbox holds a message as `sending` until the store's receipt names its
  * change; the watch then shows that change as pending, and later landed or
- * cancelled. The two arrive in either order, so the receipt is remembered by
- * identity, the change, and never matched on content. A remembered message is
- * drawn until the watch shows it, so a row cannot blink out between the reply
- * and the fold; and it lends the fold's item the key that named it while
- * sending, so one block carries the message across the receipt.
+ * cancelled. The two arrive in either order, so every message is matched by
+ * identity and never by content: the submission key the outbox minted, which
+ * core returns on the pending item and on the committed user part, and the
+ * change the receipt named. A remembered receipt is drawn until the watch
+ * shows its message, so a row cannot blink out between the reply and the
+ * fold; a receipt for a message the watch already showed draws nothing, so a
+ * message is never seen twice.
+ *
+ * State is bounded by the sends in flight and the receipts still arriving.
+ * The one read this class makes is `pending`, after a receipt whose message a
+ * snapshot could not place: the snapshot may have been taken before the submit
+ * applied, or after the message was cancelled, and only the store can say
+ * which. The read answers after the receipt, so it holds the change unless it
+ * landed or was cancelled.
  */
-import type { Oid, PendingItem, SendReceipt } from "@nyte-ai/core";
+import type { HeadName, Oid, PendingItem, SendReceipt, SessionEvent, Turn } from "@nyte-ai/core";
 import type { OutboxEntry } from "./outbox.ts";
 import { gutterRows } from "./pending-gutter.ts";
 import type { GutterRow } from "./pending-gutter.ts";
 
-interface SentMessage {
-  /** The key that named the message while it was sending. */
-  readonly key: string;
-  readonly item: PendingItem;
+export interface SentMessagesDependencies {
+  /** The head's pending changes, as `messages.pending` answers them. */
+  readonly pending: () => Promise<readonly PendingItem[]>;
+  /** The rows changed outside a watch update or an outbox change. */
+  readonly onChange: () => void;
 }
 
+/**
+ * What the watch said about a send still without a receipt: `shown` once it
+ * drew the message (pending, committed, or already settled), so the receipt
+ * draws nothing; `spanned` when a snapshot replaced the fold without it, so
+ * the receipt asks the store where it went. `shown` is final.
+ */
+type InFlightMark = "shown" | "spanned";
+
+/** The identities a watch update names for one message. */
+interface Named {
+  readonly key?: string;
+  readonly change?: Oid;
+}
+
+function userKeys(transcript: readonly Turn[]): readonly string[] {
+  const keys: string[] = [];
+  for (const turn of transcript) {
+    if (turn.kind !== "turn") continue;
+    for (const part of turn.parts) {
+      if (part.kind === "user" && part.key !== undefined) keys.push(part.key);
+    }
+  }
+  return keys;
+}
+
 export class SentMessages {
-  /** By the change the receipt named. An entry leaves with its change: landed, cancelled, or absent from a snapshot. */
-  private readonly sent = new Map<Oid, SentMessage>();
-  /**
-   * Changes the watch settled while a send was still in flight. A receipt
-   * that arrives after its change landed or was cancelled draws nothing.
-   * Cleared when the outbox drains, so it holds at most what settled during
-   * one send.
-   */
-  private readonly settledInFlight = new Set<Oid>();
-  /**
-   * Keys whose send spanned a snapshot. The snapshot may hold what their
-   * receipts name, or have landed it with no event to say so; the receipt
-   * draws nothing and the watch shows the message.
-   */
-  private readonly spannedSnapshot = new Set<string>();
+  private readonly dependencies: SentMessagesDependencies;
+  /** Receipts the watch has not shown yet, by the key that named the message while sending. */
+  private readonly arriving = new Map<string, PendingItem>();
+  /** Sends without a receipt the watch has spoken about. Pruned to the outbox's entries. */
+  private readonly inFlight = new Map<string, InFlightMark>();
+  /** Keys the outbox is still sending, as it last reported them. */
+  private sendingKeys: ReadonlySet<string> = new Set();
+  /** Keys whose `pending` read is under way; a second question waits for the first answer. */
+  private readonly verifying = new Set<string>();
 
+  constructor(dependencies: SentMessagesDependencies) {
+    this.dependencies = dependencies;
+  }
+
   /** The store answered a send. `duplicate` answers a retry whose first attempt reached the store; it is as durable as `queued`. */
   receipt(entry: OutboxEntry, receipt: SendReceipt): void {
-    if (this.spannedSnapshot.delete(entry.key)) return;
-    if (this.settledInFlight.has(receipt.change)) return;
-    this.sent.set(receipt.change, {
+    const mark = this.inFlight.get(entry.key);
+    this.inFlight.delete(entry.key);
+    if (mark === "shown") return;
+    const item: PendingItem = {
+      change: receipt.change,
+      lane: entry.lane,
+      at: entry.at,
+      content: entry.content,
       key: entry.key,
-      item: { change: receipt.change, lane: entry.lane, at: entry.at, content: entry.content },
-    });
+    };
+    this.arriving.set(entry.key, item);
+    if (mark === "spanned") this.verify(item);
   }
 
-  /** The watch landed or cancelled the change; a receipt still in flight must not draw it again. */
-  settled(change: Oid, sending: readonly OutboxEntry[]): void {
-    this.sent.delete(change);
-    if (sending.length > 0) this.settledInFlight.add(change);
+  /** The set of sending messages changed. */
+  sending(entries: readonly OutboxEntry[]): void {
+    this.sendingKeys = new Set(entries.map((entry) => entry.key));
+    for (const key of this.inFlight.keys()) {
+      if (!this.sendingKeys.has(key)) this.inFlight.delete(key);
+    }
   }
 
-  /** A snapshot replaced the fold: what it does not hold as pending is gone, and events before it were never seen. */
-  snapshot(pending: readonly PendingItem[], sending: readonly OutboxEntry[]): void {
-    for (const change of this.sent.keys()) {
-      if (!pending.some((item) => item.change === change)) this.sent.delete(change);
+  /**
+   * The watch yielded an event for `head`. A pending item, a commit that
+   * landed a submission, a landing, and a cancellation each name a message;
+   * the rest name none. Returns whether the rows changed.
+   */
+  event(event: SessionEvent, head: HeadName): boolean {
+    switch (event.kind) {
+      case "queued":
+        return event.head === head && this.shown(event.item);
+      case "commit":
+        return event.head === head && this.shown(event.item.commit);
+      case "landed":
+        return event.head === head && this.shown({ change: event.change });
+      case "queue_cancelled":
+        return this.shown({ change: event.change });
+      default:
+        return false;
     }
-    this.settledInFlight.clear();
-    for (const entry of sending) this.spannedSnapshot.add(entry.key);
   }
 
-  /** The set of sending messages changed. */
-  sending(entries: readonly OutboxEntry[]): void {
-    if (entries.length === 0) {
-      this.settledInFlight.clear();
-      this.spannedSnapshot.clear();
-      return;
+  /**
+   * A snapshot replaced the fold: events before it were never seen. A receipt
+   * it holds, pending or committed, is the watch's to draw; one it does not
+   * hold is asked about. A send without a receipt is marked by whether the
+   * snapshot placed it.
+   */
+  snapshot(pending: readonly PendingItem[], transcript: readonly Turn[]): void {
+    const keys = new Set(userKeys(transcript));
+    for (const item of pending) if (item.key !== undefined) keys.add(item.key);
+    const changes = new Set(pending.map((item) => item.change));
+    for (const [key, item] of this.arriving) {
+      if (keys.has(key) || changes.has(item.change)) this.arriving.delete(key);
+      else this.verify(item);
     }
-    for (const key of this.spannedSnapshot) {
-      if (!entries.some((entry) => entry.key === key)) this.spannedSnapshot.delete(key);
+    for (const key of this.sendingKeys) {
+      if (keys.has(key)) this.inFlight.set(key, "shown");
+      else if (this.inFlight.get(key) !== "shown") this.inFlight.set(key, "spanned");
     }
   }
 
-  /** The fold's pending items, then receipts the fold has not caught up with, then what is still sending. */
+  /**
+   * The fold's pending items, then receipts the fold has not caught up with,
+   * then what is still sending. A send the watch already showed is drawn by
+   * the fold alone, whether it shows the message pending or in the record.
+   */
   rows(pending: readonly PendingItem[], sending: readonly OutboxEntry[]): GutterRow[] {
-    const arriving = [...this.sent.values()]
-      .filter((sent) => !pending.some((item) => item.change === sent.item.change))
-      .map((sent) => sent.item);
-    return gutterRows([...pending, ...arriving], sending, (change) => this.sent.get(change)?.key);
+    const arriving = [...this.arriving.values()].filter(
+      (item) => !pending.some((shown) => shown.change === item.change || shown.key === item.key),
+    );
+    return gutterRows(
+      [...pending, ...arriving],
+      sending.filter((entry) => this.inFlight.get(entry.key) !== "shown"),
+    );
   }
+
+  private shown(named: Named): boolean {
+    let changed = false;
+    for (const [key, item] of this.arriving) {
+      if (key !== named.key && item.change !== named.change) continue;
+      this.arriving.delete(key);
+      changed = true;
+    }
+    if (
+      named.key !== undefined &&
+      this.sendingKeys.has(named.key) &&
+      this.inFlight.get(named.key) !== "shown"
+    ) {
+      // The sending row gives way to the fold's; its receipt will draw nothing.
+      this.inFlight.set(named.key, "shown");
+      changed = true;
+    }
+    return changed;
+  }
+
+  /**
+   * Ask the store whether an unplaced receipt is still pending. A result
+   * applies only to the entry it was asked about, still arriving under the
+   * same change; a message the watch placed meanwhile is left alone. Absent
+   * from the queue, the message landed (the watch shows the commit) or was
+   * cancelled, and the local row goes. A failed read keeps the row; the next
+   * snapshot asks again.
+   */
+  private verify(item: PendingItem): void {
+    const key = item.key;
+    if (key === undefined || this.verifying.has(key)) return;
+    this.verifying.add(key);
+    this.dependencies.pending().then(
+      (items) => {
+        this.verifying.delete(key);
+        if (this.arriving.get(key)?.change !== item.change) return;
+        if (items.some((queued) => queued.key === key || queued.change === item.change)) return;
+        this.arriving.delete(key);
+        this.dependencies.onChange();
+      },
+      () => {
+        this.verifying.delete(key);
+      },
+    );
+  }
 }
--- pre/packages/tui/src/sent-messages.test.ts
+++ now/packages/tui/src/sent-messages.test.ts
@@ -1,18 +1,115 @@
 import { describe, expect, test } from "bun:test";
-import type { PendingItem, SendReceipt } from "@nyte-ai/core";
+import { foldEvent, stateFromSnapshot } from "@nyte-ai/core/client";
+import type { SessionState } from "@nyte-ai/core/client";
+import { MAIN, sessionId } from "@nyte-ai/core";
+import type { PendingItem, SendReceipt, SessionEvent, SessionSnapshot, Turn } from "@nyte-ai/core";
 import { Outbox } from "./outbox.ts";
 import { SentMessages } from "./sent-messages.ts";
 
-/** Waits for the outbox's own continuations after an answer or a failure. */
+/** Waits for the outbox's and the reads' own continuations. */
 const flush = () => new Promise<void>((resolve) => setTimeout(resolve, 0));
 
+type CommitItem = Extract<SessionEvent, { kind: "commit" }>["item"];
+/** An event without its seq; the harness numbers them as the watch would. */
+type EventBody = SessionEvent extends infer E
+  ? E extends SessionEvent
+    ? Omit<E, "seq">
+    : never
+  : never;
+
+const SESSION = sessionId("sent-test");
+
+function snapshot(
+  options: {
+    readonly pending?: readonly PendingItem[];
+    readonly transcript?: readonly Turn[];
+  } = {},
+): SessionSnapshot {
+  return {
+    seq: 1,
+    head: MAIN,
+    tip:
+      options.transcript?.flatMap((turn) => (turn.kind === "turn" ? [turn.id] : [])).at(-1) ?? null,
+    session: {
+      sessionId: SESSION,
+      activation: { kind: "active" },
+      createdAt: 0,
+      lastActivityAt: 0,
+      pinned: false,
+      archived: false,
+      heads: [{ head: MAIN, tip: null }],
+      config: {},
+    },
+    config: {},
+    transcript: options.transcript ?? [],
+    pending: options.pending ?? [],
+    context: { estimatedTokens: 0, usageTokens: 0, trailingTokens: 0, contextWindow: 1000 },
+  };
+}
+
+/** A pending item as core's queue projection returns it: the change, and the key it was sent under. */
+const item = (change: string, key: string, content = "same words"): PendingItem => ({
+  change,
+  lane: "steer",
+  at: 1_000,
+  content,
+  key,
+});
+
+/** The commit a landing writes for a change, as the `commit` event carries it. */
+function landing(
+  oid: string,
+  parent: string | null,
+  change: string,
+  key: string,
+  content = "same words",
+): CommitItem {
+  return {
+    oid,
+    commit: {
+      kind: "commit",
+      parent,
+      change,
+      key,
+      run: "run-1",
+      body: { kind: "message", message: { role: "user", content, timestamp: 1_000 } },
+      at: 1_500,
+    },
+  };
+}
+
+/** The turn a snapshot carries once that commit is the record. */
+function landedTurn(oid: string, parent: string | null, key: string, content = "same words"): Turn {
+  return {
+    kind: "turn",
+    id: oid,
+    outcome: "completed",
+    startedAt: 1_500,
+    durationMs: 0,
+    parts: [{ kind: "user", commit: oid, parent, content, key }],
+  };
+}
+
 /**
- * An outbox whose store answers when the test says, so the receipt and the
- * watch can arrive in either order.
+ * Core's own fold, an outbox whose store answers when the test says, and the
+ * sent-message bridge between them, wired as the shell wires them. The
+ * receipt, the watch's frames, and the snapshot arrive in whatever order each
+ * test chooses.
  */
 function harness() {
-  const sent = new SentMessages();
   const answers: PromiseWithResolvers<SendReceipt>[] = [];
+  const reads: PromiseWithResolvers<readonly PendingItem[]>[] = [];
+  let changes = 0;
+  const sent = new SentMessages({
+    pending: () => {
+      const read = Promise.withResolvers<readonly PendingItem[]>();
+      reads.push(read);
+      return read.promise;
+    },
+    onChange: () => {
+      changes += 1;
+    },
+  });
   const keys = ["key-1", "key-2", "key-3"];
   const outbox = new Outbox({
     send: () => {
@@ -26,108 +123,275 @@
     onReceipt: (entry, receipt) => sent.receipt(entry, receipt),
     onChange: (entries) => sent.sending(entries),
   });
-  const rows = (pending: readonly PendingItem[]) =>
+  let state: SessionState = stateFromSnapshot(snapshot());
+  let seq = 1;
+  /** Folds one frame as the shell does, and says whether the rows need painting because of it. */
+  const fold = (body: EventBody): boolean => {
+    seq += 1;
+    const event: SessionEvent = { ...body, seq };
+    const outcome = foldEvent(state, event);
+    if (outcome.kind !== "state") throw new Error(`fold asked to resnapshot on ${event.kind}`);
+    state = outcome.state;
+    return sent.event(event, state.head);
+  };
+  const restore = (next: SessionSnapshot): void => {
+    state = stateFromSnapshot(next);
+    sent.snapshot(state.pending, state.transcript.items);
+  };
+  const rows = () =>
     sent
-      .rows(pending, outbox.entries)
+      .rows(state.pending, outbox.entries)
       .map((row) =>
         row.kind === "sending"
           ? `sending:${row.entry.key}`
-          : `pending:${row.item.change}${row.key === undefined ? "" : `@${row.key}`}`,
+          : `pending:${row.item.change}${row.item.key === undefined ? "" : `@${row.item.key}`}`,
       );
-  return { sent, outbox, answers, rows };
+  const transcriptKeys = () =>
+    state.transcript.items.flatMap((turn) =>
+      turn.kind === "turn"
+        ? turn.parts.flatMap((part) => (part.kind === "user" ? [part.key] : []))
+        : [],
+    );
+  return {
+    sent,
+    outbox,
+    answers,
+    reads,
+    fold,
+    restore,
+    rows,
+    transcriptKeys,
+    changes: () => changes,
+    state: () => state,
+  };
 }
 
-const item = (change: string, content = "same words"): PendingItem => ({
-  change,
-  lane: "steer",
-  at: 1_000,
-  content,
-});
-
 describe("sent messages", () => {
   test("a receipt ahead of the watch keeps one row, under one key, until the fold shows it", async () => {
-    const { outbox, answers, rows } = harness();
+    const { outbox, answers, rows, fold } = harness();
     void outbox.submit({ content: "same words", lane: "steer" });
-    expect(rows([])).toEqual(["sending:key-1"]);
+    expect(rows()).toEqual(["sending:key-1"]);
 
     answers[0]?.resolve({ kind: "queued", change: "change-1" });
     await flush();
-    expect(rows([])).toEqual(["pending:change-1@key-1"]);
-    expect(rows([item("change-1")])).toEqual(["pending:change-1@key-1"]);
+    expect(rows()).toEqual(["pending:change-1@key-1"]);
+    fold({ kind: "queued", head: MAIN, item: item("change-1", "key-1") });
+    expect(rows()).toEqual(["pending:change-1@key-1"]);
   });
 
-  test("a receipt after the change landed draws nothing", async () => {
-    const { sent, outbox, answers, rows } = harness();
+  test("the queued frame ahead of the receipt draws once; the late receipt adds nothing", async () => {
+    const { outbox, answers, rows, fold } = harness();
     void outbox.submit({ content: "same words", lane: "steer" });
-    // The watch showed the message and its turn before the store's reply came back.
-    sent.settled("change-1", outbox.entries);
+    expect(fold({ kind: "queued", head: MAIN, item: item("change-1", "key-1") })).toBe(true);
+    expect(rows()).toEqual(["pending:change-1@key-1"]);
     answers[0]?.resolve({ kind: "queued", change: "change-1" });
     await flush();
-    expect(rows([])).toEqual([]);
+    expect(rows()).toEqual(["pending:change-1@key-1"]);
   });
 
-  test("a receipt after the change was cancelled draws nothing", async () => {
-    const { sent, outbox, answers, rows } = harness();
+  test("the commit frame ahead of its landed frame moves the message to the record without a pending twin", async () => {
+    const { outbox, answers, rows, fold, state, transcriptKeys } = harness();
     void outbox.submit({ content: "same words", lane: "steer" });
-    sent.settled("change-1", outbox.entries);
     answers[0]?.resolve({ kind: "queued", change: "change-1" });
     await flush();
-    expect(rows([])).toEqual([]);
-    // What settled during that send is forgotten once nothing is sending.
-    void outbox.submit({ content: "same words", lane: "steer" });
-    answers[1]?.resolve({ kind: "queued", change: "change-1" });
+    fold({ kind: "queued", head: MAIN, item: item("change-1", "key-1") });
+    fold({ kind: "head_moved", head: MAIN, from: null, to: "commit-1", reason: "land" });
+    fold({ kind: "commit", head: MAIN, item: landing("commit-1", null, "change-1", "key-1") });
+    expect(transcriptKeys()).toEqual(["key-1"]);
+    expect(state().pending).toEqual([]);
+    expect(rows()).toEqual([]);
+    fold({ kind: "landed", head: MAIN, change: "change-1" });
+    expect(rows()).toEqual([]);
+  });
+
+  test("a receipt after the change landed draws nothing", async () => {
+    const { outbox, answers, rows, fold } = harness();
+    void outbox.submit({ content: "same words", lane: "steer" });
+    // The watch showed the message and its turn before the store's reply came back.
+    fold({ kind: "queued", head: MAIN, item: item("change-1", "key-1") });
+    fold({ kind: "head_moved", head: MAIN, from: null, to: "commit-1", reason: "land" });
+    fold({ kind: "commit", head: MAIN, item: landing("commit-1", null, "change-1", "key-1") });
+    fold({ kind: "landed", head: MAIN, change: "change-1" });
+    expect(rows()).toEqual([]);
+    answers[0]?.resolve({ kind: "queued", change: "change-1" });
     await flush();
-    expect(rows([])).toEqual(["pending:change-1@key-2"]);
+    expect(rows()).toEqual([]);
   });
 
+  test("a receipt after the change was cancelled draws nothing, and the next send starts clean", async () => {
+    const { outbox, answers, rows, fold } = harness();
+    void outbox.submit({ content: "same words", lane: "steer" });
+    fold({ kind: "queued", head: MAIN, item: item("change-1", "key-1") });
+    fold({ kind: "queue_cancelled", change: "change-1" });
+    answers[0]?.resolve({ kind: "queued", change: "change-1" });
+    await flush();
+    expect(rows()).toEqual([]);
+    void outbox.submit({ content: "same words", lane: "steer" });
+    answers[1]?.resolve({ kind: "queued", change: "change-2" });
+    await flush();
+    expect(rows()).toEqual(["pending:change-2@key-2"]);
+  });
+
   test("a duplicate receipt for a retry draws the message the first attempt made durable", async () => {
     const { outbox, answers, rows } = harness();
     void outbox.submit({ content: "same words", lane: "steer" });
     answers[0]?.reject(new Error("connection reset"));
     await flush();
-    expect(rows([])).toEqual(["sending:key-1"]);
+    expect(rows()).toEqual(["sending:key-1"]);
     answers[1]?.resolve({ kind: "duplicate", change: "change-1" });
     await flush();
-    expect(rows([])).toEqual(["pending:change-1@key-1"]);
+    expect(rows()).toEqual(["pending:change-1@key-1"]);
   });
 
   test("a duplicate receipt for a change that landed during the retries draws nothing", async () => {
-    const { sent, outbox, answers, rows } = harness();
+    const { outbox, answers, rows, fold } = harness();
     void outbox.submit({ content: "same words", lane: "steer" });
     answers[0]?.reject(new Error("connection reset"));
     await flush();
-    sent.settled("change-1", outbox.entries);
+    fold({ kind: "queued", head: MAIN, item: item("change-1", "key-1") });
+    fold({ kind: "head_moved", head: MAIN, from: null, to: "commit-1", reason: "land" });
+    fold({ kind: "commit", head: MAIN, item: landing("commit-1", null, "change-1", "key-1") });
+    fold({ kind: "landed", head: MAIN, change: "change-1" });
     answers[1]?.resolve({ kind: "duplicate", change: "change-1" });
     await flush();
-    expect(rows([])).toEqual([]);
+    expect(rows()).toEqual([]);
   });
 
   test("identical prompts stay apart by change and key, never by content", async () => {
-    const { outbox, answers, rows } = harness();
+    const { outbox, answers, rows, fold } = harness();
     void outbox.submit({ content: "same words", lane: "steer" });
     void outbox.submit({ content: "same words", lane: "steer" });
     answers[0]?.resolve({ kind: "queued", change: "change-1" });
     answers[1]?.resolve({ kind: "queued", change: "change-2" });
     await flush();
-    expect(rows([item("change-1")])).toEqual(["pending:change-1@key-1", "pending:change-2@key-2"]);
-    expect(rows([item("change-1"), item("change-2")])).toEqual([
-      "pending:change-1@key-1",
-      "pending:change-2@key-2",
-    ]);
+    fold({ kind: "queued", head: MAIN, item: item("change-1", "key-1") });
+    expect(rows()).toEqual(["pending:change-1@key-1", "pending:change-2@key-2"]);
+    fold({ kind: "queued", head: MAIN, item: item("change-2", "key-2") });
+    expect(rows()).toEqual(["pending:change-1@key-1", "pending:change-2@key-2"]);
+    fold({ kind: "head_moved", head: MAIN, from: null, to: "commit-1", reason: "land" });
+    fold({ kind: "commit", head: MAIN, item: landing("commit-1", null, "change-1", "key-1") });
+    expect(rows()).toEqual(["pending:change-2@key-2"]);
   });
 
-  test("a snapshot drops what it does not hold and mutes receipts for sends that spanned it", async () => {
-    const { sent, outbox, answers, rows } = harness();
+  test("a snapshot taken before the submit applied keeps the message until the queued frame follows", async () => {
+    const { outbox, answers, rows, reads, fold, restore, changes } = harness();
     void outbox.submit({ content: "same words", lane: "steer" });
+    restore(snapshot());
+    expect(rows()).toEqual(["sending:key-1"]);
     answers[0]?.resolve({ kind: "queued", change: "change-1" });
     await flush();
+    // The receipt is durable, the snapshot did not hold it: the store is asked, and the row stays meanwhile.
+    expect(rows()).toEqual(["pending:change-1@key-1"]);
+    expect(reads).toHaveLength(1);
+    reads[0]?.resolve([item("change-1", "key-1")]);
+    await flush();
+    expect(rows()).toEqual(["pending:change-1@key-1"]);
+    expect(changes()).toBe(0);
+    fold({ kind: "queued", head: MAIN, item: item("change-1", "key-1") });
+    expect(rows()).toEqual(["pending:change-1@key-1"]);
+  });
+
+  test("a snapshot taken after the message landed shows the record alone, before and after the receipt", async () => {
+    const { outbox, answers, rows, reads, restore, transcriptKeys } = harness();
     void outbox.submit({ content: "same words", lane: "steer" });
-    // The snapshot holds neither: the first landed before it, the second is still on its way.
-    sent.snapshot([], outbox.entries);
-    expect(rows([])).toEqual(["sending:key-2"]);
+    restore(snapshot({ transcript: [landedTurn("commit-1", null, "key-1")] }));
+    expect(transcriptKeys()).toEqual(["key-1"]);
+    expect(rows()).toEqual([]);
+    answers[0]?.resolve({ kind: "queued", change: "change-1" });
+    await flush();
+    expect(rows()).toEqual([]);
+    expect(reads).toHaveLength(0);
+
+    // The same landing, seen after the receipt: the snapshot places the arriving row and it goes.
+    void outbox.submit({ content: "same words", lane: "steer" });
     answers[1]?.resolve({ kind: "queued", change: "change-2" });
     await flush();
-    expect(rows([])).toEqual([]);
-    expect(rows([item("change-2")])).toEqual(["pending:change-2"]);
+    expect(rows()).toEqual(["pending:change-2@key-2"]);
+    restore(
+      snapshot({
+        transcript: [
+          landedTurn("commit-1", null, "key-1"),
+          landedTurn("commit-2", "commit-1", "key-2"),
+        ],
+      }),
+    );
+    expect(rows()).toEqual([]);
+    expect(reads).toHaveLength(0);
   });
+
+  test("a snapshot that holds the message as pending adopts the arriving row", async () => {
+    const { outbox, answers, rows, reads, restore } = harness();
+    void outbox.submit({ content: "same words", lane: "steer" });
+    answers[0]?.resolve({ kind: "queued", change: "change-1" });
+    await flush();
+    restore(snapshot({ pending: [item("change-1", "key-1")] }));
+    expect(rows()).toEqual(["pending:change-1@key-1"]);
+    expect(reads).toHaveLength(0);
+  });
+
+  test("a message cancelled before the snapshot leaves once the store confirms it is gone", async () => {
+    const { outbox, answers, rows, reads, restore, changes } = harness();
+    void outbox.submit({ content: "same words", lane: "steer" });
+    answers[0]?.resolve({ kind: "queued", change: "change-1" });
+    await flush();
+    void outbox.submit({ content: "same words", lane: "steer" });
+    // Another client cancelled the first; the second was still on its way when the snapshot was read.
+    restore(snapshot());
+    expect(rows()).toEqual(["pending:change-1@key-1", "sending:key-2"]);
+    expect(reads).toHaveLength(1);
+    reads[0]?.resolve([]);
+    await flush();
+    expect(rows()).toEqual(["sending:key-2"]);
+    expect(changes()).toBe(1);
+
+    answers[1]?.resolve({ kind: "queued", change: "change-2" });
+    await flush();
+    expect(rows()).toEqual(["pending:change-2@key-2"]);
+    expect(reads).toHaveLength(2);
+    reads[1]?.resolve([]);
+    await flush();
+    expect(rows()).toEqual([]);
+    expect(changes()).toBe(2);
+  });
+
+  test("a stale answer never removes a message the watch has since placed", async () => {
+    const { outbox, answers, rows, reads, fold, restore, changes } = harness();
+    void outbox.submit({ content: "same words", lane: "steer" });
+    answers[0]?.resolve({ kind: "queued", change: "change-1" });
+    await flush();
+    restore(snapshot());
+    expect(reads).toHaveLength(1);
+    fold({ kind: "queued", head: MAIN, item: item("change-1", "key-1") });
+    reads[0]?.resolve([]);
+    await flush();
+    expect(rows()).toEqual(["pending:change-1@key-1"]);
+    expect(changes()).toBe(0);
+  });
+
+  test("a failed read keeps the row; the next snapshot asks again", async () => {
+    const { outbox, answers, rows, reads, restore } = harness();
+    void outbox.submit({ content: "same words", lane: "steer" });
+    answers[0]?.resolve({ kind: "queued", change: "change-1" });
+    await flush();
+    restore(snapshot());
+    reads[0]?.reject(new Error("store busy"));
+    await flush();
+    expect(rows()).toEqual(["pending:change-1@key-1"]);
+    restore(snapshot());
+    expect(reads).toHaveLength(2);
+    reads[1]?.resolve([]);
+    await flush();
+    expect(rows()).toEqual([]);
+  });
+
+  test("a redelivered message keeps its key: the copy's queued frame answers the original's receipt", async () => {
+    const { outbox, answers, rows, fold } = harness();
+    void outbox.submit({ content: "same words", lane: "steer" });
+    fold({ kind: "queued", head: MAIN, item: item("change-1", "key-1") });
+    fold({ kind: "queue_cancelled", change: "change-1" });
+    fold({ kind: "queued", head: MAIN, item: { ...item("change-2", "key-1"), lane: "queue" } });
+    answers[0]?.resolve({ kind: "queued", change: "change-1" });
+    await flush();
+    expect(rows()).toEqual(["pending:change-2@key-1"]);
+  });
 });
--- pre/packages/tui/src/pending-gutter.ts
+++ now/packages/tui/src/pending-gutter.ts
@@ -27,24 +27,20 @@
 
 /**
  * One row: a durable pending change, or a message the outbox is still sending.
- * A pending row this outbox sent keeps the key that named it while it was
- * sending, so what draws it can keep the same block across the receipt.
+ * A pending item carries the submission key that named it while sending, so
+ * what draws it can keep the same block across the receipt.
  */
 export type GutterRow =
-  | { readonly kind: "pending"; readonly item: PendingItem; readonly key?: string }
+  | { readonly kind: "pending"; readonly item: PendingItem }
   | { readonly kind: "sending"; readonly entry: OutboxEntry };
 
 /** Durable first, oldest first; then what is still on its way. */
 export function gutterRows(
   pending: readonly PendingItem[],
   sending: readonly OutboxEntry[],
-  keyOf: (change: PendingItem["change"]) => string | undefined = () => undefined,
 ): GutterRow[] {
   return [
-    ...pending.map((item): GutterRow => {
-      const key = keyOf(item.change);
-      return key === undefined ? { kind: "pending", item } : { kind: "pending", item, key };
-    }),
+    ...pending.map((item): GutterRow => ({ kind: "pending", item })),
     ...sending.map((entry): GutterRow => ({ kind: "sending", entry })),
   ];
 }
--- pre/packages/tui/src/pending-gutter.test.ts
+++ now/packages/tui/src/pending-gutter.test.ts
@@ -154,8 +154,7 @@
 
 const pending = (change: string, lane: string, content: string, key?: string): GutterRow => ({
   kind: "pending",
-  item: { change, lane, content, at: 2_000 },
-  ...(key === undefined ? {} : { key }),
+  item: { change, lane, content, at: 2_000, ...(key === undefined ? {} : { key }) },
 });
 
 describe("steer messages at the transcript's tail", () => {
--- pre/packages/tui/src/pending-tail.ts
+++ now/packages/tui/src/pending-tail.ts
@@ -6,9 +6,9 @@
  * Admission then changes that row in place and moves nothing, so the message
  * is never seen in two places.
  *
- * A message keeps one block from sending through pending: the outbox key that
- * named it while sending stays an alias of the block once the receipt names
- * its change. Follow-ups queued with ctrl+enter take the compact rows in
+ * A message keeps one block from sending through pending: the submission key
+ * that named it while sending stays an alias of the block once the receipt
+ * names its change, and core returns that key on the pending item itself. Follow-ups queued with ctrl+enter take the compact rows in
  * `pending-gutter.ts` instead.
  */
 import { BoxRenderable, fg, StyledText, TextRenderable } from "@opentui/core";
@@ -21,11 +21,11 @@
 import { appendUser, repaintTree } from "./transcript.ts";
 import type { Transcript } from "./transcript.ts";
 
-/** Every name a row answers to; a pending row this outbox sent also answers to its sending key. */
+/** Every name a row answers to; a pending item also answers to the key it was submitted under. */
 function rowIds(row: GutterRow): readonly string[] {
   if (row.kind === "sending") return [`key:${row.entry.key}`];
   const ids = [`change:${row.item.change}`];
-  if (row.key !== undefined) ids.push(`key:${row.key}`);
+  if (row.item.key !== undefined) ids.push(`key:${row.item.key}`);
   return ids;
 }
 
--- pre/packages/tui/src/interactive.ts
+++ now/packages/tui/src/interactive.ts
@@ -909,7 +909,12 @@
       sessionId: info.sessionId,
       follower,
       outbox,
-      sent: new SentMessages(),
+      sent: new SentMessages({
+        pending: () => this.host.nyte.messages.pending({ sessionId: info.sessionId, head: MAIN }),
+        onChange: () => {
+          if (current !== undefined && this.session === current) this.syncGutter(current);
+        },
+      }),
       config,
       state: {
         sessionId: info.sessionId,
@@ -991,7 +996,7 @@
     const { state } = session;
     const event = update.kind === "event" ? update.event : undefined;
     const snapshot = update.kind === "snapshot";
-    if (snapshot) session.sent.snapshot(state.pending, session.outbox.entries);
+    if (snapshot) session.sent.snapshot(state.pending, state.transcript.items);
     // Fold and react to every event, but reconcile only the latest visual state
     // before a frame. A resnapshot must still reset even if deltas follow it.
     this.pendingVisual = {
@@ -1007,6 +1012,7 @@
     this.tasks.update(state, event);
     if (event === undefined) return;
     this.tuiPlugins.emit(event);
+    if (session.sent.event(event, state.head)) this.syncGutter(session);
     switch (event.kind) {
       case "activation_changed":
         return;
@@ -1034,8 +1040,6 @@
       case "effect":
         return;
       case "commit":
-        if (event.item.commit.change !== undefined)
-          session.sent.settled(event.item.commit.change, session.outbox.entries);
         if (event.head !== state.head) return;
         if (event.item.commit.body.kind === "message") {
           if (event.item.commit.body.message.role === "user") {
@@ -1083,8 +1087,6 @@
         return;
       case "landed":
       case "queue_cancelled":
-        session.sent.settled(event.change, session.outbox.entries);
-        return;
       case "queued":
       case "head_moved":
       case "stack":
--- pre/packages/docs/content/docs/design.mdx
+++ now/packages/docs/content/docs/design.mdx
@@ -596,10 +596,14 @@
 1. Mint a key on Enter, keep the message locally as `sending`, and render it immediately.
 2. Submit with that key. On a network failure, keep the row and retry with backoff using the same
    key until it succeeds or the user cancels the local send.
-3. Treat `queued` and `duplicate` as durable. Replace the local row with the returned change oid.
+3. Treat `queued` and `duplicate` as durable. Match the local row to the store's message by
+   identity, never by content: the receipt names the change, and the key rides on the change,
+   on its pending item, and on the commit that lands it. The receipt and the watch arrive in
+   either order; a message the watch already shows draws nothing from its late receipt.
 
 The idempotency ref and queue tip move in one CAS. A retry after a lost response returns the first
-change and never enqueues the message twice.
+change and never enqueues the message twice. The key is correlation for the sender, not
+authorization, and stays outside the message the model reads.
 
 ### Who runs?
 
--- pre/packages/core/src/client/session-state.ts
+++ now/packages/core/src/client/session-state.ts
@@ -149,6 +149,17 @@
   });
 }
 
+function withoutPending(
+  items: readonly PendingItem[],
+  change: Oid | undefined,
+): readonly PendingItem[] {
+  if (change === undefined || !items.some((item) => item.change === change)) return items;
+  return mergeQueuedLanes(
+    items.filter((item) => item.change !== change),
+    { lane: (entry) => entry.lane, compare: comparePending },
+  );
+}
+
 /**
  * Apply one event. Events for other heads only touch what is head-neutral
  * (facts, deletion). The seq advances with every event but is never a reconnect
@@ -167,11 +178,15 @@
       const transcript = appendTranscriptCommit(state.transcript, event.item);
       if (transcript === undefined) return { kind: "resnapshot" };
       const reached = state.expectedTip === transcript.tip;
+      // The head and the queue base move in one CAS but arrive as separate
+      // frames; the change leaves the queue with the commit that landed it, so
+      // no frame shows the message both pending and in the transcript.
       return {
         kind: "state",
         state: {
           ...base,
           transcript,
+          pending: withoutPending(state.pending, event.item.commit.change),
           overlay: foldLiveParts(state.overlay, event),
           expectedTip: reached ? undefined : state.expectedTip,
         },
@@ -217,24 +232,12 @@
       if (event.head !== state.head) return { kind: "state", state: base };
       return {
         kind: "state",
-        state: {
-          ...base,
-          pending: mergeQueuedLanes(
-            state.pending.filter((item) => item.change !== event.change),
-            { lane: (entry) => entry.lane, compare: comparePending },
-          ),
-        },
+        state: { ...base, pending: withoutPending(state.pending, event.change) },
       };
     case "queue_cancelled":
       return {
         kind: "state",
-        state: {
-          ...base,
-          pending: mergeQueuedLanes(
-            state.pending.filter((item) => item.change !== event.change),
-            { lane: (entry) => entry.lane, compare: comparePending },
-          ),
-        },
+        state: { ...base, pending: withoutPending(state.pending, event.change) },
       };
     case "effect": {
       if (state.run?.runId !== event.runId) return { kind: "state", state: base };
--- pre/packages/core/test/kernel/sdk.test.ts
+++ now/packages/core/test/kernel/sdk.test.ts
@@ -393,6 +393,85 @@
   }
 });
 
+test("a submission key follows the message from the queue into the record, outside its content", async () => {
+  let release: (() => void) | undefined;
+  const opened = new Promise<void>((resolve) => {
+    release = resolve;
+  });
+  const nyte = await open(echo({ gate: () => opened }));
+  try {
+    const { sessionId: id } = await nyte.sessions.create();
+    nyte.attach();
+    await nyte.messages.send({ sessionId: id, content: "first" });
+    await collect(nyte, { sessionId: id, afterSeq: 0 }, (event) => event.kind === "text_delta");
+    const before = await nyte.sessions.snapshot({ sessionId: id });
+    assert.ok(before !== undefined);
+
+    const keyed = await nyte.messages.send({
+      sessionId: id,
+      content: "keyed",
+      lane: "queue",
+      key: "outbox-1",
+    });
+    assert.equal(keyed.kind, "queued");
+    const queuedItem = (await nyte.messages.pending({ sessionId: id })).find(
+      (item) => item.change === keyed.change,
+    );
+    assert.equal(queuedItem?.key, "outbox-1");
+    const held = await nyte.sessions.snapshot({ sessionId: id });
+    assert.deepEqual(
+      held?.pending.map((item) => [item.change, item.key]),
+      [[keyed.change, "outbox-1"]],
+    );
+
+    release?.();
+    const events = await collect(
+      nyte,
+      { sessionId: id, afterSeq: before.seq },
+      (event) => event.kind === "landed" && event.change === keyed.change,
+    );
+    const queued = events.find((event) => event.kind === "queued");
+    assert.ok(queued?.kind === "queued");
+    assert.deepEqual([queued.item.change, queued.item.key], [keyed.change, "outbox-1"]);
+    const landing = events.find(
+      (event) => event.kind === "commit" && event.item.commit.change === keyed.change,
+    );
+    assert.ok(landing?.kind === "commit");
+    assert.equal(landing.item.commit.key, "outbox-1");
+    // The key is commit metadata: the model's message is untouched.
+    assert.ok(landing.item.commit.body.kind === "message");
+    assert.deepEqual(Object.keys(landing.item.commit.body.message).sort(), [
+      "content",
+      "role",
+      "timestamp",
+    ]);
+    assert.equal(landing.item.commit.body.message.content, "keyed");
+
+    assert.deepEqual(await nyte.runs.wait({ sessionId: id }), { kind: "idle" });
+    const after = await nyte.sessions.snapshot({ sessionId: id });
+    const userParts =
+      after?.transcript.flatMap((turn) =>
+        turn.kind === "turn"
+          ? turn.parts.flatMap((part) =>
+              part.kind === "user" ? [[part.commit, part.content, part.key]] : [],
+            )
+          : [],
+      ) ?? [];
+    assert.deepEqual(
+      userParts.map(([, content, key]) => [content, key]),
+      [
+        ["first", undefined],
+        ["keyed", "outbox-1"],
+      ],
+    );
+    assert.equal(userParts[1]?.[0], landing.item.oid);
+    assert.deepEqual(after?.pending, []);
+  } finally {
+    release?.();
+    await nyte.close();
+  }
+});
+
 test("a tool that asks a question parks the run; the answer wakes it and the run finishes", async () => {
   const nyte = await open();
   try {
--- pre/packages/protocol/test/contract.test.ts
+++ now/packages/protocol/test/contract.test.ts
@@ -151,6 +151,27 @@
   assert.ok(issues.some((issue) => issue.path.startsWith("/item/commit/body")));
 });
 
+test("a submission key rides on the pending item, the commit, and the user part, and stays optional", () => {
+  const pending = { change: "abc", lane: "steer", at: 1, content: "hi" };
+  assert.ok(Value.Check(schemas.PendingItem, pending));
+  assert.ok(Value.Check(schemas.PendingItem, { ...pending, key: "outbox-1" }));
+  assert.ok(!Value.Check(schemas.PendingItem, { ...pending, key: 7 }));
+  const commit = {
+    kind: "commit",
+    parent: null,
+    change: "abc",
+    key: "outbox-1",
+    body: { kind: "message", message: { role: "user", content: "hi", timestamp: 1 } },
+    at: 1,
+  };
+  assert.ok(Value.Check(schemas.Commit, commit));
+  assert.ok(!Value.Check(schemas.Commit, { ...commit, key: null }));
+  const part = { kind: "user", commit: "abc", parent: null, content: "hi" };
+  assert.ok(Value.Check(schemas.UserTurnPart, part));
+  assert.ok(Value.Check(schemas.UserTurnPart, { ...part, key: "outbox-1" }));
+  assert.ok(!Value.Check(schemas.UserTurnPart, { ...part, key: 7 }));
+});
+
 test("strict inputs refuse unknown keys, including an own __proto__ key parsed from JSON", () => {
   const input = OPERATIONS["sessions.get"].input;
   assert.ok(Value.Check(input, { sessionId: "s" }));
