diff --git a/chunk-bun-9gqvxy8c.js b/chunk-bun-9gqvxy8c.js
index a688fc1ab712acd2bdff182911b88472aed527b9..9eaa8194871bdaa07c17ef0ee6daa97fd7ce5bdc 100644
--- a/chunk-bun-9gqvxy8c.js
+++ b/chunk-bun-9gqvxy8c.js
@@ -11434,7 +11434,12 @@ class OptimizedBuffer {
   }
   fillRect(x, y, width, height, bg2) {
     this.guard();
-    this.lib.bufferFillRect(this.bufferPtr, x, y, width, height, bg2);
+    const left = Math.max(0, x);
+    const top = Math.max(0, y);
+    const right = Math.min(this._width, x + width);
+    const bottom = Math.min(this._height, y + height);
+    if (right <= left || bottom <= top) return;
+    this.lib.bufferFillRect(this.bufferPtr, left, top, right - left, bottom - top, bg2);
   }
   colorMatrix(matrix, cellMask, strength = 1, target = 3 /* Both */) {
     this.guard();
diff --git a/chunk-node-f647ts9q.js b/chunk-node-f647ts9q.js
index 79ac5288da91205e1a61ff1efe9640f7e242cb5d..3cc44461eeef6dc27ee20c9b1882d251f3d5c08f 100644
--- a/chunk-node-f647ts9q.js
+++ b/chunk-node-f647ts9q.js
@@ -11413,7 +11413,12 @@ class OptimizedBuffer {
   }
   fillRect(x, y, width, height, bg2) {
     this.guard();
-    this.lib.bufferFillRect(this.bufferPtr, x, y, width, height, bg2);
+    const left = Math.max(0, x);
+    const top = Math.max(0, y);
+    const right = Math.min(this._width, x + width);
+    const bottom = Math.min(this._height, y + height);
+    if (right <= left || bottom <= top) return;
+    this.lib.bufferFillRect(this.bufferPtr, left, top, right - left, bottom - top, bg2);
   }
   colorMatrix(matrix, cellMask, strength = 1, target = 3 /* Both */) {
     this.guard();
